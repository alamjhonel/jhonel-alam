# Dependencies
node_modules/
.pnp
.pnp.js

# Build output
dist/
dist-ssr/
build/
*.local

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Environment files (never commit secrets)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Editor / OS
.DS_Store
Thumbs.db
.vscode/*
!.vscode/extensions.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.cache/

# Vite cache
.vite/
*.tsbuildinfo

# Optional: lockfile-less local npm cache
.npm/
.yarn/
.pnpm-store/

# Cert PDF originals — these get inlined as base64 data URIs at build
# time and bundled into the JS. Keeping the source PDFs out of git
# avoids leaking credentials in the public repo history.
src/assets/certs/*.pdf
